spring:
  application:
    name: practica-4
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true