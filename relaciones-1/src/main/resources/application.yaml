spring:
  application:
    name: relaciones-1
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
  sql:
    init:
      mode: always
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  data:
    web:
      pageable:
        default-page-size: 20
        max-page-size: 100
